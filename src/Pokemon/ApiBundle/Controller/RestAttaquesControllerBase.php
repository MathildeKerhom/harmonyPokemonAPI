<?php

namespace Pokemon\ApiBundle\Controller;

use FOS\RestBundle\Controller\Annotations as Rest;
use FOS\RestBundle\Request\ParamFetcherInterface;

use Nelmio\ApiDocBundle\Annotation\ApiDoc;

use Symfony\Component\HttpFoundation\Request;

use Pokemon\ApiBundle\Form\AttaquesType;
use Pokemon\ApiBundle\Entity\Attaques;
use Pokemon\ApiBundle\Controller\RestTypesController;
use Pokemon\ApiBundle\Controller\RestSyncController as Controller;

/**
 * Attaques Repository
 *
 * This class was generated by the Harmony / Doctrine ORM.
 *
 * This class will be regenerated by Harmony. DO NOT MODIFIY THIS CLASS.
 */
class RestAttaquesControllerBase extends Controller {

    public function getNewEntity() {
        return new Attaques();
    }

    /**=========================================================================
     * CRUD REST
     *========================================================================*/

    /**
     * GET /{id}
     *
     * @param string attaquesId
     * @return Response
     * @Rest\Get("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD Attaques",
     *   description="Show entity"
     * )
     * @return type
     */
    public function getAttaquesAction($id)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $data = $entityManager->getRepository('PokemonApiBundle:Attaques')->findOneById($id);
        $view = $this->view($data, 200);
        return $this->handleView($view);
    }

    /**
     * POST
     *
     * @Rest\Post("")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD Attaques",
     *   description="Create entity",
     *   requirements={
     *      {
     *          "name"="nom",
     *          "dataType"="string",
     *          "requirement"="",
     *          "description"="The nom attaques",
     *      },
     *      {
     *          "name"="puissance",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The puissance attaques",
     *      },
     *      {
     *          "name"="precis",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The precis attaques",
     *      },
     *      {
     *          "name"="type",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The type attaques",
     *      },
     *   }
     * )
     * @return type
     */
    public function createAttaquesAction(Request $request) {
        $entity = new Attaques();

        $form = $this->createForm(new AttaquesType(), $entity, array(
            'allow_extra_fields' => true
        ));
        $form->submit($request);

        if ($form->isValid()) {
            $entityManager = $this->getDoctrine()->getManager();
            $entityManager->persist($entity);
            $entityManager->flush();
            $view = $this->view($entity, 200);
        } else {
            $view = $this->view(array("error" => "Parameters entity isn't valid" . $form->getErrors()), 400);
        }

        return $this->handleView($view);
    }

    /**
     * GET
     *
     * @Rest\Get("")
     * @Rest\QueryParam(name="page", nullable=true, requirements="\d+", default="0", description="Number of the page")
     * @Rest\QueryParam(name="quantity", nullable=true, requirements="\d+", default="0", description="Quantity of entities per page")
     * @param ParamFetcher $paramFetcher
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD Attaques",
     *   description="Show all entities",
     *   requirements={
     *   }
     * )
     * @return type
     */
    public function getAllAttaquesAction(ParamFetcherInterface $paramFetcher)
    {
        $repository = $this->getDoctrine()->getRepository('PokemonApiBundle:Attaques');

        $page = $paramFetcher->get('page');
        $quantity = $paramFetcher->get('quantity');

        $data = $repository->getList($page, $quantity);

        if ($data) {
            $view = $this->view(array("Attaquess" => $data), 200);
        } else {
            $view = $this->view(null, 204);
        }

        return $this->handleView($view);
    }

    /**
     * PUT /{id}
     *
     * @Rest\Put("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD Attaques",
     *   description="Update entity",
     *   requirements={
     *      {
     *          "name"="nom",
     *          "dataType"="string",
     *          "requirement"="",
     *          "description"="The nom attaques",
     *      },
     *      {
     *          "name"="puissance",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The puissance attaques",
     *      },
     *      {
     *          "name"="precis",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The precis attaques",
     *      },
     *      {
     *          "name"="type",
     *          "dataType"="integer",
     *          "requirement"="",
     *          "description"="The type attaques",
     *      },
     *   }
     * )
     * @return type
     */
    public function updateAttaquesAction($id, Request $request)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $entity = $entityManager->getRepository('PokemonApiBundle:Attaques')
            ->findOneById($id);

        $form = $this->createForm(new AttaquesType(), $entity, array(
            'allow_extra_fields' => true
        ));

        $form->submit($request, false);

        if ($form->isValid()) {
            $entityManager = $this->getDoctrine()->getManager();
            $entityManager->persist($entity);
            $entityManager->flush();
            $view = $this->view($entity, 200);
        } else {
            $view = $this->view(array("error" => "Parameters entity isn't valid"), 400);
        }

        return $this->handleView($view);
    }

    /**
     * DELETE /{id}
     *
     * @Rest\Delete("/{id}")
     * @ApiDoc(
     *   resource=true,
     *   section="Rest CRUD Attaques",
     *   description="Delete entity",
     * )
     * @return type
     */
    public function deleteAttaquesAction($id)
    {
        $entityManager = $this->getDoctrine()->getManager();
        $entity = $entityManager->getRepository('PokemonApiBundle:Attaques')
            ->find(array("id" => $id));

        if (!$entity) {
            throw $this->createNotFoundException('Unable to find Attaques entity');
        }

        $entityManager->remove($entity);
        $entityManager->flush();

        $view = $this->view($entity, 200);
        return $this->handleView($view);
    }


}

?>